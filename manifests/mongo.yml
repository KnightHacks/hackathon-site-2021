apiVersion: v1
kind: Service
metadata:
  name: kh-mongo
spec:
  ports:
    - port: 27017
      protocol: TCP
  selector:
    app: kh-mongo
  type: ClusterIP
---
apiVersion: v1
kind: PersistantVolumeClaim
metadata:
  name: kh-mongo-claim
spec:
  storageClassName: azureDisk
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
---
apiVersion: v1
kind: Pod
metadata:
  name: kh-mongo
  labels:
    app: kh-mongo
spec:
  volumes:
    - name: kh-mongo-data
      persistantVolumeClaim:
        claimName: kh-mongo-claim
  containers:
    - name: kh-mongo
      image: mongo
      ports:
        - containerPort: 27017
      volumeMounts:
        - mountPath: "/data/db"
          name: kh-mongo-data